# هذا الملف يوجه كل الطلبات إلى backend/public/index.php
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /api/
    
    # توجيه كل الطلبات إلى مجلد الباك-أند الفعلي
    RewriteRule ^(.*)$ ../../backend/public/index.php [L]
</IfModule>
